apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scraper-output
  labels:
    app: scraper
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
