manager:
  collectorImage:
    repository: otel/opentelemetry-collector-k8s
  resources:
    requests: { cpu: 50m, memory: 64Mi }
    limits:   { cpu: 200m, memory: 128Mi }


admissionWebhooks:
  certManager:
    enabled: true

crds:
  create: true